# Scrapers + pipeline
requests>=2.32
psycopg[binary]>=3.2
python-dotenv>=1.0
anthropic>=0.40
# Evenemang (LibCal-bibliotek m.fl.)
icalendar>=6.0            # RFC 5545 iCal/ICS-parsning
# County-möten (CivicEngage/CivicPlus Agenda Center)
civic-scraper>=0.4      # strukturerad metadata från AgendaCenter-sajter
pdfplumber>=0.11        # extraherar text ur agenda-PDF:er
# För kommande parsers (Stage 0/2):
beautifulsoup4>=4.12    # HTML-scraping (SmartGov, county alerts)
# Innehållsspår v1: artikel-illustrationer
Pillow>=10.0            # normaliserar leverantörsbilder (t.ex. fal.ai:s JPEG) till PNG
